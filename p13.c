
#include<stdlib.h>
 struct node
  Rheader=(struct node*)malloc(sizeof(struct node));
    Rheader->link=NULL;
    Rheader->exp=0;
    Rheader->coef=0;
   Rptr=Rheader;
{    int coef,exp;
     struct node *link;};
void main()
{
    int n2,n1,i,expo,coefi;    struct node *Pheader,*Qheader,*Rheader,*temp,*Pptr,*Qptr,*Rptr,*neww;

        Rheader=(struct node*)malloc(sizeof(struct node));
    Rheader->link=NULL;
    Rheader->exp=0;
->coef=0;
  GNU nano 2.2.6                                       File: p13.c                                                                                    

#include<stdio.h>
#include<stdlib.h>
 struct node
  Rheader=(struct node*)malloc(sizeof(struct node));
    Rheader->link=NULL
    Rheader->exp=0;
    Rheader->coef=0;
   Rptr=Rheder;
{    int coef,exp;
     struct n;
};
void main()
{
    int n2,n1,i,expo,coefi;
    struct nde *Pheader,*Qheader,*Rheader,*temp,*Pptr,*Qptr,*Rptr,*neww;

        Rheader=(struct node*)malloc(sizeof(struct node));
    Rheader-link=NULL;
    Rheader->exp=0;
    Rheader->coef=0;
   Rptr=Rheader;

    printf("enter the number of terms\n");
    scanf("%d",&n1);
    Pheader(struct node*)malloc(sizeof(struct node));
    Pheader->link=NULL;
    Pheader->exp=0;
    Pheader->tr=Pheader;
    printf("enter the details\n");
    for(i=0;i<n1;i++)
       {  

         temp=(struct node*)malloc(sizeof(struct node));
          printf("enter the exponent: \n");
          canf("%d",&expo);
          printf("enter the coefficient\n");
          scanf("%d",&coefi);
                                                                  [ Read 143 lines ]
^G Get Help              ^O WriteOut              ^R Read File             ^Y Prev Page             ^K Cut Text              ^C Cur Pos
^               ^J Justify               ^W Where Is              ^V Next Page             ^U UnCut Text            ^T To Spell
   Rptr=Rheader;

    printf("enter the number of terms\n");
    scanf("%d",&n1);
   Pheader=(struct node*)malloc(sizeof(struct node));
    Pheader->link=NULL;
    Pheader->exp=0;
    Phader->coef=0;
   Pptr=Pheader;
    printf("enter the details\n");
    for(i=0;i<n1;i++)
       {  
         temp=(struct node*)malloc(sizeof(struct node));
          printf("enter the exponent: \n");          scanf("%d",&expo);
          printf("enter the coefficient\n");
          scanf("%d",&coefi);
          temp->link=NULL;
          temp->exp=expo;
          temp->coef=coefi;
          Pptr->link=temp;
          Pptr=Pptr->link;
         }
       printf("enter the number of terms\n");
    scanf("%d",&n2);
    Qheader=(struct node*)malloc(sizeof(struct node));
    Qheader->link=NULL;
    Qheader->exp=0;
    Qheader->coef=0;
    Qptr=Qheader
    printf("enter the details\n");
    for(i=0;i<n2;i++)
       { 
          
       struct node*)malloc(sizeof(struct node));
          printf("enter the exponent: \n");
          scanf("%d",&expo);
          printf("enter the coefficient\n");
          scanf("%d",&coefi);
  GNU ano 2.2.6                                                                      [ Read 143 lines ]
